[tool.poetry]
name = "mlops0823"
version = "0.1.0"
description = ""
authors = ["rgdnk <unscore@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
